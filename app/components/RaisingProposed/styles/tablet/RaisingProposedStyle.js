import { StyleSheet } from 'react-native';

const RaisingProposedStyles = StyleSheet.create({
  headingTitle: {
    fontSize: 20,
  },
  criteriaValue: {
    fontSize: 20,
  }
});

export default RaisingProposedStyles;