import { StyleSheet } from 'react-native';

const TipStyles = StyleSheet.create({
  title: {
    marginBottom: 0,
    flex: 1,
  },
  viewDetailLabel: {
    fontSize: 16,
  },
  viewDetailIcon: {
    fontSize: 24,
  }
});

export default TipStyles;